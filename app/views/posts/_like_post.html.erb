<% @post ||= post %>
<%= form_with(model: current_user.like_post_relationships.build, remote: true) do |f| %>
<div><%= hidden_field_tag :post_id, @post.id %></div>
<%= f.submit "favorite_border", class: "material-icons" %>
<% end %>
