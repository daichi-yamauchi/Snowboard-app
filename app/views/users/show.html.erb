<% provide(:title, @user.name) %>
<div class="d-flex p-3">

    <img src="<%= gravatar_for @user %>" alt="<%= @user.name %>" class="rounded-circle w-100px">
    <div class="ml-3">
        <h3>
            <%= @user.name %>
        </h3>
    </div>
</div>
